Claro, te proporcionaré una lista de ejercicios que puedes realizar tanto en Python como en JavaScript. Recuerda, Python y JavaScript son lenguajes de programación con soporte de programación funcional, pero no son puramente funcionales, como Haskell o Erlang. 

Los ejercicios están destinados a proporcionarte una comprensión de cómo puedes aplicar los conceptos de programación funcional en estos lenguajes. 

1. Escribe una función que toma un número y devuelve su cuadrado.
2. Escribe una función que toma una lista y devuelve una nueva lista con todos los elementos duplicados.
3. Escribe una función que filtra una lista de números a solo números impares.
4. Escribe una función que toma una lista de números y retorna el producto de todos los números.
5. Escribe una función que toma una lista de cadenas y concatena todas las cadenas.
6. Escribe una función que toma un número y calcula su factorial.



---------AQUI ARRIBA LO TERMINADO----------




7. Escribe una función que toma una lista de números y retorna la suma de todos los números.
8. Escribe una función que recibe una lista de listas y las aplanas a una sola lista.
9. Escribe una función que retorna una lista de números y devuelve el número más grande.
10. Escribe una función que toma una lista de números y devuelve el número más pequeño.
11. Escribe una función que recibe una lista de números y devuelve la lista ordenada de menor a mayor.
12. Escribe una función que toma una lista y devuelve una nueva lista con los elementos invertidos.
13. Escribe una función que tome una lista de números y devuelva el promedio.
14. Escribe una función que tome una cadena y devuelva la misma cadena pero con cada palabra capitalizada.
15. Escribe una función que tome una cadena y devuelva la misma cadena pero invertida.
16. Escribe una función que tome una lista y un número y devuelva `True` si el número está en la lista y `False` si no.
17. Escribe una función que toma dos listas y devuelve una nueva lista con los elementos que se encuentran en ambas listas.
18. Escribe una función que toma una lista y una función y aplica la función a cada elemento de la lista.
19. Escribe una función que toma una lista y una función y filtra la lista basándose en la función.
20. Escribe una función que toma un número y una lista de números y devuelve una lista de los números más cercanos al número dado.
21. Escribe una función que toma una cadena y devuelve una nueva cadena sin espacios.
22. Escribe una función que toma una cadena y una letra, y devuelve el número de veces que aparece la letra en la cadena.
23. Escribe una función que toma una lista de números y una función, y devuelve la suma de aplicar la función a cada número.
24. Escribe una función que toma una lista de listas y devuelve la longitud de la lista más larga.
25. Escribe una función que toma una lista de cadenas y devuelve la cadena más larga.
26. Escribe una función que toma una lista de números y devuelve el segundo número más grande.
27. Escribe una función que tome una cadena y devuelva la frecuencia de cada palabra en la cadena.
28. Escribe una función que toma una lista de números y devuelve la lista sin números duplicados.
29. Escribe una función que toma una cadena y devuelve la longitud de la palabra más larga.
30. Escribe una función que tome una lista y devuelve una nueva lista sin los elementos en posiciones impares.
31. Escribe una función que toma dos listas y devuelve `True` si las listas tienen al menos un elemento en común.
32. Escribe una función que tome una lista y una función, y devuelva el primer elemento de la lista que cumpla con la función.
33. Escribe una función que toma una lista y devuelve la suma de los cuadrados de todos los números en la lista.
34. Escribe una función que toma una cadena y devuelve una nueva cadena con todas las vocales eliminadas.
35. Escribe una función que tome una cadena y devuelva `True` si la cadena es un palíndromo.
36. Escribe una función que tome una lista de números y devuelve la suma de los números pares.
37. Escribe una función que tome una lista de números y devuelva una nueva lista con los números ordenados de mayor a menor.
38. Escribe una función que tome una lista de números y devuelva la diferencia entre el número más grande y el más pequeño.
39. Escribe una función que tome una lista de cadenas y devuelva una nueva lista con las cadenas ordenadas por longitud.
40. Escribe una función que tome una lista y una función, y devuelva una nueva lista con la función aplicada a cada elemento de la lista.
41. Escribe una función que tome una cadena y devuelva una nueva cadena con las palabras en orden inverso.
42. Escribe una función que toma una lista de números y devuelve la mediana.
43. Escribe una función que tome una lista de números y devuelva la moda.
44. Escribe una función que toma una lista y un número, y devuelva una nueva lista con el número añadido al final.
45. Escribe una función que toma una cadena y una palabra, y devuelve `True` si la palabra está en la cadena.
46. Escribe una función que toma una lista y devuelve `True` si la lista contiene solo números pares.
47. Escribe una función que toma una lista y una función, y devuelve la suma de aplicar la función a los elementos de la lista que cumplen con la función.
48. Escribe una función que toma una cadena y una letra, y devuelve la cadena con todas las ocurrencias de la letra eliminada.
49. Escribe una función que toma una lista y un número, y devuelve la lista con el número añadido al principio.
50. Escribe una función que toma una lista de números y devuelve una nueva lista con los números impares al principio y los pares al final.

Espero que estos ejercicios te ayuden a practicar la programación funcional en Python y JavaScript.
